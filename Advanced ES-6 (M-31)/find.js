// if each element full fill the condition but return only first element which fullfill the condition
const numbers = [56, 78, 56, 43, 86]
const num = numbers.find(N => N > 60)
console.log(num)

/*
  1. Map (do for each element and return as an array)
  2. ForEach ( do for each element but no returns)
  3. Filter (which values full fill the condition return as an array)
  4. Find (if each values full fill the condition but returns only only the this value which filled the  condition first )
*/ 