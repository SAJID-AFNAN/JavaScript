const apple = 350
let orange = 270 
var tomato = 100
let me = 1000

// apple = 300     //const variable is fixed. is not changeable

/*
Always use let,const, not use var .
because var has scope issue
*/

console.table([apple,orange,tomato,me]);

console.log(typeof undefined);  //undefined
console.log(typeof null);  //object
console.log(typeof NaN);  //Number